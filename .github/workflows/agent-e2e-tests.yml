name: Agent/Reason End-to-End Tests

on:
  workflow_call:

jobs:
  reason-qwen-grpo-test:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export MEGATRON_PATH=/workspace/dataset/Megatron-LM
          # One venv per engine: sglang and vllm pin the same kernel libraries to
          # different versions, so a shared venv breaks whichever is installed first.
          bash requirements/install.sh agentic --engine sglang
          bash requirements/install.sh agentic --venv .venv-vllm --engine vllm

      - name: Megatron SGLang Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-grpo-collocated-mg-sgl

      - name: Megatron vLLM Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv-vllm/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-grpo-collocated-mg-vllm

      - name: Megatron SGLang Pipeline mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-grpo-pipeline-mg-sgl

      - name: Megatron vLLM Pipeline mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv-vllm/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-grpo-pipeline-mg-vllm

      - name: FSDP SGLang Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-grpo-collocated-fsdp-sgl

      - name: FSDP vLLM Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv-vllm/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-grpo-collocated-fsdp-vllm

      - name: FSDP SGLang Pipeline mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-grpo-pipeline-fsdp-sgl

      - name: FSDP vLLM Pipeline mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv-vllm/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-grpo-pipeline-fsdp-vllm

      - name: Clean up
        run: |
          rm -rf .venv .venv-vllm
          find /workspace/results -maxdepth 3 -type d \( -name checkpoints -o -name converted_ckpts \) \
               -prune -exec rm -rf {} + 2>/dev/null || true
          uv cache prune


  agentlightning-test:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export MEGATRON_PATH=/workspace/dataset/Megatron-LM
          bash requirements/install.sh agentic
          
      - name: Install dependencies
        run: |
          export UV_LINK_MODE=copy
          source .venv/bin/activate
          # litellm 1.95 imports get_flat_dependant, which fastapi dropped in 0.140.7
          uv pip install "agentlightning==0.3.0" "litellm<1.95" "autogen-agentchat" "autogen-ext[openai]" "mcp>=1.10.0" "mcp-server-calculator" "openai==2.32.0"
          # tensorboard 2.21+ requires protobuf>=6.31.1; autogen pins protobuf~=5.29.x
          uv pip install "tensorboard<2.21"

      - name: AgentLightning calc_x multiturn 
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/agentlightning/run.sh

      - name: Clean up
        run: |
          rm -rf .venv
          find /workspace/results -maxdepth 3 -type d \( -name checkpoints -o -name converted_ckpts \) \
               -prune -exec rm -rf {} + 2>/dev/null || true
          uv cache prune


  reason-qwen-grpo-test-rollout-logprobs:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export MEGATRON_PATH=/workspace/dataset/Megatron-LM
          # One venv per engine: sglang and vllm pin the same kernel libraries to
          # different versions, so a shared venv breaks whichever is installed first.
          bash requirements/install.sh agentic --engine sglang
          bash requirements/install.sh agentic --venv .venv-vllm --engine vllm

      - name: Megatron SGLang Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-grpo-collocated-mg-sgl-rollout-logprobs

      - name: Megatron vLLM Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv-vllm/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-grpo-collocated-mg-vllm-rollout-logprobs

      - name: Megatron SGLang Pipeline mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-grpo-pipeline-mg-sgl-rollout-logprobs

      - name: Megatron vLLM Pipeline mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv-vllm/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-grpo-pipeline-mg-vllm-rollout-logprobs

      - name: FSDP SGLang Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-grpo-collocated-fsdp-sgl-rollout-logprobs

      - name: FSDP vLLM Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv-vllm/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-grpo-collocated-fsdp-vllm-rollout-logprobs

      - name: Clean up
        run: |
          rm -rf .venv .venv-vllm
          find /workspace/results -maxdepth 3 -type d \( -name checkpoints -o -name converted_ckpts \) \
               -prune -exec rm -rf {} + 2>/dev/null || true
          uv cache prune

  reason-qwen-ppo-test:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export MEGATRON_PATH=/workspace/dataset/Megatron-LM
          bash requirements/install.sh agentic

      - name: Megatron SGLang Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-ppo-collocated-mg-sgl.yaml

      - name: Clean up
        run: |
          rm -rf .venv
          find /workspace/results -maxdepth 3 -type d \( -name checkpoints -o -name converted_ckpts \) \
               -prune -exec rm -rf {} + 2>/dev/null || true
          uv cache prune

  reason-qwen-importance-sampling-test:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export MEGATRON_PATH=/workspace/dataset/Megatron-LM
          bash requirements/install.sh agentic
  
      - name: Megatron SGLang Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-is-collocated-mg-sgl

      - name: Clean up
        run: |
          rm -rf .venv
          find /workspace/results -maxdepth 3 -type d \( -name checkpoints -o -name converted_ckpts \) \
               -prune -exec rm -rf {} + 2>/dev/null || true
          uv cache prune

  reason-qwen-reinforcepp-test:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export MEGATRON_PATH=/workspace/dataset/Megatron-LM
          bash requirements/install.sh agentic
  
      - name: Megatron SGLang Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-reinpp-collocated-mg-sgl

      - name: Clean up
        run: |
          rm -rf .venv
          find /workspace/results -maxdepth 3 -type d \( -name checkpoints -o -name converted_ckpts \) \
               -prune -exec rm -rf {} + 2>/dev/null || true
          uv cache prune

  coding-online-rl-qwen-ppo-test:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export MEGATRON_PATH=/workspace/dataset/Megatron-LM
          bash requirements/install.sh agentic

      - name: Install dependencies
        run: |
          source .venv/bin/activate
          uv pip install httpx asyncio fuzzywuzzy

      - name: SGLang Pipeline mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/agent/coding_online_rl/run.sh

      - name: Clean up
        run: |
          rm -rf .venv
          find /workspace/results -maxdepth 3 -type d \( -name checkpoints -o -name converted_ckpts \) \
               -prune -exec rm -rf {} + 2>/dev/null || true
          uv cache prune

  rstar2-qwen-grpo-test:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export MEGATRON_PATH=/workspace/dataset/Megatron-LM
          bash requirements/install.sh agentic

      - name: Clone code-judge
        uses: actions/checkout@v5
        with:
          repository: 0xWJ/code-judge
          ref: fbc5293afa4fa0965418ed5b0d3979fe1d815a49
          path: code-judge

      - name: Install dependencies
        run: |
          source .venv/bin/activate
          apt-get update -y && apt-get install redis -y
          uv pip install -r code-judge/requirements.txt
          uv pip install -e code-judge
          uv pip install math_verify
          uv pip install sympy
          uv pip install pylatexenc
          tmux kill-session -t server 2>/dev/null || true
          tmux kill-session -t worker 2>/dev/null || true
          pkill redis-server 2>/dev/null || true

      - name: SGLang Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          redis-server --daemonize yes --protected-mode no --bind 0.0.0.0 --port 6380
          tmux new-session -d -s server \
          'source .venv/bin/activate && \
            cd code-judge && \
            MAX_EXECUTION_TIME=4 \
            REDIS_URI="redis://$MASTER_ADDR:6380" \
            RUN_WORKERS=0 \
            uvicorn app.main:app --host 0.0.0.0 --port 8088 --workers 16 \
            2>&1 | tee server.log' 
          tmux new-session -d -s worker \
          'source .venv/bin/activate && \
            cd code-judge && \
            MAX_EXECUTION_TIME=4 \
            REDIS_URI="redis://$MASTER_ADDR:6380" \
            MAX_WORKERS=64 \
            python run_workers.py \
            2>&1 | tee worker.log'
          sleep 10
          echo ""
          echo "=== Server log (after 10s) ==="
          if [ -f code-judge/server.log ]; then
            cat code-judge/server.log
          else
            echo "⚠️  server.log not found"
            tmux capture-pane -t server -p || echo "Could not capture server pane"
          fi
          echo ""
          echo "=== Worker log (after 10s) ==="
          if [ -f code-judge/worker.log ]; then
            cat code-judge/worker.log
          else
            echo "⚠️  worker.log not found"
            tmux capture-pane -t worker -p || echo "Could not capture worker pane"
          fi
          tmux ls
          bash tests/e2e_tests/agent/rstar2/run.sh

      - name: Clean up
        run: |
          rm -rf .venv
          find /workspace/results -maxdepth 3 -type d \( -name checkpoints -o -name converted_ckpts \) \
               -prune -exec rm -rf {} + 2>/dev/null || true
          uv cache prune
          tmux kill-session -t server 2>/dev/null || true
          tmux kill-session -t worker 2>/dev/null || true
          pkill redis-server 2>/dev/null || true
          rm -rf code-judge/

  searchr1-qwen-grpo-test:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export MEGATRON_PATH=/workspace/dataset/Megatron-LM
          bash requirements/install.sh agentic

      - name: Train with SGLang Megatron Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/agent/searchr1/run-train.sh qwen2.5-1.5b-megatron

      - name: Evaluate with SGLang Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/agent/searchr1/run-eval.sh qwen2.5-1.5b-eval

      - name: Clean up
        run: |
          rm -rf .venv
          find /workspace/results -maxdepth 3 -type d \( -name checkpoints -o -name converted_ckpts \) \
               -prune -exec rm -rf {} + 2>/dev/null || true
          uv cache prune

  wideseek-r1-qwen-grpo-test:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export MEGATRON_PATH=/workspace/dataset/Megatron-LM
          bash requirements/install.sh agentic

      - name: Train with SGLang Megatron Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/agent/wideseek/run-train.sh

      - name: Evaluate with SGLang Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/agent/wideseek/run-eval.sh

      - name: Clean up
        run: |
          rm -rf .venv
          find /workspace/results -maxdepth 3 -type d \( -name checkpoints -o -name converted_ckpts \) \
               -prune -exec rm -rf {} + 2>/dev/null || true
          uv cache prune

  qwen-vl-grpo-test:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export MEGATRON_PATH=/workspace/dataset/Megatron-LM
          # One venv per engine: sglang and vllm pin the same kernel libraries to
          # different versions, so a shared venv breaks whichever is installed first.
          bash requirements/install.sh agentic --engine sglang
          bash requirements/install.sh agentic --venv .venv-vllm --engine vllm

      - name: FSDP SGLang Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-vl-3b-grpo-collocated-fsdp-sgl

      - name: FSDP vLLM Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv-vllm/bin/activate
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-vl-3b-grpo-collocated-fsdp-vllm

      - name: Clean up
        run: |
          rm -rf .venv .venv-vllm
          find /workspace/results -maxdepth 3 -type d \( -name checkpoints -o -name converted_ckpts \) \
               -prune -exec rm -rf {} + 2>/dev/null || true
          uv cache prune

  qwen3-vl-grpo-test:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export MEGATRON_PATH=/workspace/dataset/Megatron-LM
          bash requirements/install.sh agentic --torch 2.8.0 --sglang 0.5.4 --transformers 4.57.1 --no-apex

      - name: FSDP SGLang Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          export LD_LIBRARY_PATH=.venv/lib/python3.11/site-packages/nvidia/cuda_runtime/lib/:$LD_LIBRARY_PATH
          bash tests/e2e_tests/reasoning/run.sh qwen3-vl-2b-grpo-collocated-fsdp-sgl

      - name: Clean up
        run: |
          rm -rf .venv
          find /workspace/results -maxdepth 3 -type d \( -name checkpoints -o -name converted_ckpts \) \
               -prune -exec rm -rf {} + 2>/dev/null || true
          uv cache prune

  qwen-moe-grpo-test:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export MEGATRON_PATH=/workspace/dataset/Megatron-LM
          bash requirements/install.sh agentic

      - name: MoE sequential MLP Collocated mode 
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          cleanup() {
            ray stop --force || true
            rm -rf /workspace/results/grpo-moe-CI-test-seqmlp-2.5b/converted_ckpts/
          }
          cleanup
          trap cleanup EXIT
          bash tests/e2e_tests/reasoning/run.sh qwen3-moe-2.5b-collocated-mg-sgl-sequentialmlp
      
      - name: MoE TeGroupgemm MLP Collocated mode 
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          cleanup() {
            ray stop --force || true
            rm -rf /workspace/results/grpo-moe-CI-test-seqmlp-2.5b/converted_ckpts/
          }
          cleanup
          trap cleanup EXIT
          bash tests/e2e_tests/reasoning/run.sh qwen3-moe-2.5b-collocated-mg-sgl-tegroupgemm

      - name: MoE model parallel ep test 
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          cleanup() {
            ray stop --force || true
            rm -rf /workspace/results/grpo-moe-CI-test-seqmlp-2.5b/converted_ckpts/
          }
          cleanup
          trap cleanup EXIT
          bash tests/e2e_tests/reasoning/run.sh qwen3-moe-2.5b-collocated-mg-sgl-ep-test
        
      - name: MoE model parallel tpe test 
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          cleanup() {
            ray stop --force || true
            rm -rf /workspace/results/grpo-moe-CI-test-seqmlp-2.5b/converted_ckpts/
          }
          cleanup
          trap cleanup EXIT
          bash tests/e2e_tests/reasoning/run.sh qwen3-moe-2.5b-collocated-mg-sgl-tpe-test
      
      - name: MoE model parallel tp test 
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          cleanup() {
            ray stop --force || true
            rm -rf /workspace/results/grpo-moe-CI-test-seqmlp-2.5b/converted_ckpts/
          }
          cleanup
          trap cleanup EXIT
          bash tests/e2e_tests/reasoning/run.sh qwen3-moe-2.5b-collocated-mg-sgl-tp-test

      - name: qwen3 dense model parallel tp test 
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          cleanup() {
            ray stop --force || true
            rm -rf /workspace/results/grpo-qwen3-4b-CI-test/converted_ckpts/
          }
          cleanup
          trap cleanup EXIT
          bash tests/e2e_tests/reasoning/run.sh qwen3-4b-collocated-mg-sgl-test

      - name: Clean up
        run: |
          rm -rf .venv
          find /workspace/results -maxdepth 3 -type d \( -name checkpoints -o -name converted_ckpts \) \
               -prune -exec rm -rf {} + 2>/dev/null || true
          uv cache prune

  fusco-moe-qwen-grpo-test:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export MEGATRON_PATH=/workspace/dataset/Megatron-LM
          bash requirements/install.sh agentic

      - name: Clone FUSCO
        uses: actions/checkout@v5
        with:
          repository: infinigence/FUSCO
          path: FUSCO

      - name: Install FUSCO
        run: |
          source .venv/bin/activate
          cd FUSCO
          uv pip install . --no-build-isolation
          mkdir -p lib
          apt-get update -y && apt-get install curl -y
          curl -L -o lib/libfusco.so https://gh-proxy.com/https://github.com/infinigence/FUSCO/releases/download/v0.1/libfusco-torch2.11.so
          cd ..

      - name: MoE FUSCO ep test
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          export FUSCO_SO_PATH=$(pwd)/FUSCO/lib/libfusco.so
          source .venv/bin/activate
          cleanup() {
            ray stop --force || true
            rm -rf /workspace/results/grpo-moe-CI-test-seqmlp-2.5b/converted_ckpts/
          }
          cleanup
          trap cleanup EXIT
          bash tests/e2e_tests/reasoning/run.sh qwen3-moe-2.5b-collocated-mg-sgl-ep-fusco-test

      - name: Clean up
        run: |
          rm -rf .venv
          rm -rf FUSCO
          find /workspace/results -maxdepth 3 -type d \( -name checkpoints -o -name converted_ckpts \) \
               -prune -exec rm -rf {} + 2>/dev/null || true
          uv cache prune

  megatron-bridge-qwen-grpo-test:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          export MEGATRON_PATH=/workspace/dataset/Megatron-LM
          bash requirements/install.sh agentic
          uv pip install transformers==4.57.1
          # --no-deps: bitsandbytes' torch requirement is unpinned, so a plain
          # install re-resolves torch through pyproject's override and replaces
          # the version install.sh chose for this stack.
          uv pip install --no-deps bitsandbytes

      - name: Train with SGLang Megatron-Bridge Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          export PYTHONPATH=/workspace/dataset/Megatron-Bridge-0.3.0/src:$PYTHONPATH
          export PYTHONPATH=/workspace/dataset/Megatron-LM-b0cc2706ddc60d2aefd5fff346445b5c013036a8:$PYTHONPATH
          export CUDA_DEVICE_MAX_CONNECTIONS=1
          bash tests/e2e_tests/reasoning/run.sh qwen2.5-1.5b-grpo-collacated-mbridge-sgl
          rm -rf /workspace/results/grpo-1.5b-megatron-mbridge/

      - name: Clean up
        run: |
          rm -rf .venv
          find /workspace/results -maxdepth 3 -type d \( -name checkpoints -o -name converted_ckpts \) \
               -prune -exec rm -rf {} + 2>/dev/null || true
          uv cache prune

  moonlight-mbridge-grpo-test:
    runs-on: reason
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Create reason environment
        run: |
          unset UV_DEFAULT_INDEX
          export UV_PATH=/workspace/dataset/.uv
          export UV_LINK_MODE=symlink
          export UV_CACHE_DIR=/workspace/dataset/.uv_cache
          export UV_PYTHON_INSTALL_DIR=/workspace/dataset/.uv_python
          bash requirements/install.sh agentic

      - name: Train Moonlight-16B with SGLang Megatron-Bridge Collocated mode
        timeout-minutes: 20
        run: |
          export REPO_PATH=$(pwd)
          source .venv/bin/activate
          export PYTHONPATH=/workspace/dataset/Megatron-Bridge-0.4.2/src:$PYTHONPATH
          export PYTHONPATH=/workspace/dataset/Megatron-LM-core0.17:$PYTHONPATH
          export CUDA_DEVICE_MAX_CONNECTIONS=1
          bash tests/e2e_tests/reasoning/run.sh moonlight-16b-grpo-mbridge-sgl
          rm -rf /workspace/results/moonlight-16b-megatron-mbridge/

      - name: Clean up
        run: |
          rm -rf .venv
          uv cache prune
