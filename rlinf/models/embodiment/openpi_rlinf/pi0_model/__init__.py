# Copyright 2026 The RLinf Authors.
#
# Vendored, self-contained PyTorch OpenPI 0.5 model core.
#
# These modules are copied (and import-namespaced) from
# openpi-comet-pytorch-mixed/src/openpi/models_pytorch_new so that the embodied
# eval path does not depend on the externally installed ``openpi`` package and
# does not patch the ``transformers`` library. Keep file contents close to the
# upstream source; only intra-package imports are rewritten to relative imports.
